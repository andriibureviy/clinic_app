<%= form_tag view_doctors_user_path(current_user), method: :get do %>
  <%= select_tag :category_id, options_for_select(Category.all.collect { |c| [c.name, c.id] }) %>
  <%= submit_tag "Filter by category" %>
<% end %>

<% @doctors.each do |doctor| %>
  <%= doctor.name %>
<% end %>