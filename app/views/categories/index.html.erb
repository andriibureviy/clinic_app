<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.collection_check_boxes :doctors_ids, Doctor.all, :id, :name %>

  <%= f.label :categories %>
  <%= f.collection_check_boxes :category_ids, Category.all, :id, :name %>

  <%= f.submit %>
<% end %>